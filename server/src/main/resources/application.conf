ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.harsh.fittrack.ApplicationKt.module ]
    }
}

jwt {
    secret = "dev-secret-change-in-production"
    secret = ${?JWT_SECRET}
}
