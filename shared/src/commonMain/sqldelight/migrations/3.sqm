CREATE TABLE IF NOT EXISTS PersonalRecordEntity (
    exerciseId  TEXT    NOT NULL PRIMARY KEY,
    maxWeightKg REAL    NOT NULL,
    maxReps     INTEGER NOT NULL,
    achievedAt  INTEGER NOT NULL
);
