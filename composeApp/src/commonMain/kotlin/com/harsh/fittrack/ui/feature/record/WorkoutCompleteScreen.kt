package com.harsh.fittrack.ui.feature.record

import androidx.compose.runtime.Composable

/** Full-screen celebration + summary. Save → Home. Discard → confirm dialog → Home. */
@Composable
fun WorkoutCompleteScreen(
    onSave: () -> Unit,
    onDiscard: () -> Unit,
) {
    // TODO
}
